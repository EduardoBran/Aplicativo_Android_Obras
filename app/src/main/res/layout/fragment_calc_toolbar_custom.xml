<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingEnd="4dp"
    tools:ignore="RtlSymmetry">

    <ImageButton
        android:id="@+id/btnToolbarExport"
        android:layout_width="@dimen/width_icon_toolbar_calc_download"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/export_summary_title_calc"
        android:padding="12dp"
        android:src="@drawable/ic_download_28"
        android:visibility="gone"
        app:tint="@color/white"
        tools:visibility="visible" />

    <ImageButton
        android:id="@+id/btnToolbarHome"
        android:layout_width="@dimen/width_icon_toolbar_calc_home"
        android:layout_height="48dp"
        android:layout_marginEnd="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/calc_home_title"
        android:padding="12dp"
        android:src="@drawable/ic_home_28"
        app:tint="@color/white" />
</LinearLayout>