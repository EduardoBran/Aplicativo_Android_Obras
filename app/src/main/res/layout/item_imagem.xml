<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="8dp"
    app:cardUseCompatPadding="true">

    <!-- 🔁 Usa ConstraintLayout para fixar razão de aspecto do thumb -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Área da foto com razão fixa (ex.: quadrado 1:1; pode trocar para 4:3, 16:9, etc.) -->
        <ImageView
            android:id="@+id/imgThumb"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:contentDescription="@string/imagens_title"
            android:scaleType="centerCrop"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/ic_image_placeholder" />

        <!-- SHIMMER sobre a área da foto -->
        <com.facebook.shimmer.ShimmerFrameLayout
            android:id="@+id/shimmerThumb"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:clickable="false"
            android:focusable="false"
            android:importantForAccessibility="no"
            android:visibility="gone"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:shimmer_auto_start="false"
            app:shimmer_base_alpha="0.9"
            app:shimmer_dropoff="0.5"
            app:shimmer_highlight_alpha="0.6"
            app:shimmer_intensity="0.3">

            <View
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/img_placeholder_square" />
        </com.facebook.shimmer.ShimmerFrameLayout>

        <!-- Botão de zoom sobreposto no canto do thumb -->
        <ImageButton
            android:id="@+id/btnExpand"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_margin="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/detail"
            android:src="@drawable/ic_zoom_in"
            app:layout_constraintEnd_toEndOf="@id/imgThumb"
            app:layout_constraintTop_toTopOf="@id/imgThumb"
            app:tint="@android:color/white"
            tools:ignore="ContentDescription" />

        <!-- Nome -->
        <TextView
            android:id="@+id/tvNome"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:paddingHorizontal="8dp"
            android:paddingTop="8dp"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/imgThumb"
            tools:text="Nome da Imagem" />

        <!-- Data -->
        <TextView
            android:id="@+id/tvData"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:fontFamily="@font/roboto"
            android:fontFeatureSettings="tnum"
            android:gravity="center"
            android:letterSpacing="0"
            android:paddingHorizontal="8dp"
            android:paddingBottom="8dp"
            android:textColor="@color/text_secondary_recycler"
            android:textSize="14sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tvNome"
            tools:text="20/08/2025" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>